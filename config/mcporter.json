{
	"mcpServers": {
		"yandex-tracker": {
			"description": "Yandex Tracker MCP",
			"command": "uvx",
			"args": ["yandex-tracker-mcp@latest"],
			"env": {
				"TRACKER_TOKEN": "$env:TRACKER_TOKEN",
				"TRACKER_CLOUD_ORG_ID": "$env:TRACKER_CLOUD_ORG_ID",
				"PATH": "$env:PATH"
			}
		}
	}
}
